import { ZUSTAND } from "../../../types/IZustand"

export const createCheckoutSlice = (
    set: any
): ZUSTAND.IChatingState => ({
    

})

let set: any
export const bindSet = (_set: any) => {
    set = _set
}