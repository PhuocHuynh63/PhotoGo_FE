import React from "react";

export default function VendorTab({ campaignId }: { campaignId: string }) {
    // TODO: fetch API lấy vendor theo campaignId
    return <div>Tab Mời Vendor (ID: {campaignId})</div>;
} 